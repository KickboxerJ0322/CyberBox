import { describe,expect,it,afterEach } from 'vitest';import request from 'supertest';import { createApp } from '../src/app.js';import { DemoLabManager } from '../src/demoManager.js';
const managers:DemoLabManager[]=[];afterEach(async()=>{await Promise.all(managers.map(m=>m.shutdown()));managers.length=0});
function setup(){const manager=new DemoLabManager();managers.push(manager);return{manager,app:createApp(manager)}}
describe('CyberBox API',()=>{it('reports health',async()=>{const{app}=setup();const r=await request(app).get('/api/health');expect(r.status).toBe(200);expect(r.body.status).toBe('ok')});it('starts and stops a lab',async()=>{const{app}=setup();const start=await request(app).post('/api/labs');expect(start.status).toBe(201);expect(start.body.status).toBe('running');const stop=await request(app).delete(`/api/labs/${start.body.sessionId}`);expect(stop.status).toBe(200)});it('returns a safe AI fallback',async()=>{const{app}=setup();const start=await request(app).post('/api/labs');const r=await request(app).post(`/api/labs/${start.body.sessionId}/explain`).send({lessonId:3,command:'nmap target',output:'3000/tcp open http'});expect(r.status).toBe(200);expect(r.body.details.length).toBeGreaterThan(0)})});
